<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/instance_item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="12dp"
    android:paddingVertical="10dp"
    android:layout_marginTop="4dp"
    android:background="@drawable/bg_instance_item"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <ImageView
        android:id="@+id/instance_icon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/ic_minecraft_cube"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/instance_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/on_surface"
            android:textSize="14sp"
            android:textStyle="bold"
            android:singleLine="true"
            android:ellipsize="end"
            android:fontFamily="@font/misans" />

        <TextView
            android:id="@+id/instance_version"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="1dp"
            android:textColor="@color/text_secondary"
            android:textSize="11sp"
            android:singleLine="true"
            android:ellipsize="end"
            android:fontFamily="@font/misans" />
    </LinearLayout>

    <TextView
        android:id="@+id/instance_tag"
        android:layout_width="wrap_content"
        android:layout_height="22dp"
        android:paddingHorizontal="8dp"
        android:gravity="center"
        android:textSize="10sp"
        android:textStyle="bold"
        android:fontFamily="@font/misans"
        android:textColor="@color/primary"
        android:background="@drawable/bg_mod_menu_toggle"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/instance_check"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginStart="8dp"
        android:src="@drawable/ic_check"
        android:tint="@color/primary"
        android:visibility="gone" />

</LinearLayout>
